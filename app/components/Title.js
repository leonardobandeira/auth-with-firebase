export default function Title({ title }) {
  return (
    <span
      className="text-black font-bold text-2xl shadow-lg
      border-4 border-transparent border-b-black"
    >
      {title}
    </span>
  );
}
